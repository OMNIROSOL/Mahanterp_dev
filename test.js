const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const ledgers = await prisma.stockLedger.groupBy({ by: ['sourceDocumentId', 'itemId'], _count: { id: true }, having: { id: { _count: { gt: 1 } } } }); console.log(ledgers); } main().catch(console.error).finally(() => prisma.$disconnect());
